# Explicit routes in _redirects keep canonical URLs extensionless without
# Netlify's Pretty URLs post-processing adding a trailing slash.
[build.processing.html]
  pretty_urls = false
[build]
  command = "pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
